#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

typedef struct NODE {
	int data;
	struct NODE* link;
} NODE;

int main(int argc, char* argv[]) {
	NODE* p1;
	//	p1 = (NODE *)malloc(sizeof(NODE));
	//
	//	p1->data = 10;
	//	p1->link = NULL;
	//
	//	NODE *p2;
	//
	//	p2 = (NODE *)malloc(sizeof(NODE));
	//	p2->data = 20;
	//	p2->link = NULL;
	//	p1->link = p2;

	//	while(1){
			//p1=(NODE*)malloc(10240);
			//free(p1);
	//	}

	//	while(p1->link != NULL){
	//		pritnf("%d",p->data);
	//		p1=p1->link;
	//	}
	//
	//	printf("%d",p1->data);
	//
	//	free(p1);
	//	free(p2);

	return 0;
}